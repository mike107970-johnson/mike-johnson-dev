{
  "name": "footballvows",
  "version": "1.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch server.mjs",
    "start": "node server.mjs",
    "build": "node --check server.mjs && node --check public/app.js && node --check public/data.js && node --check worker/index.mjs",
    "check": "node --check server.mjs && node --check public/app.js && node --check public/data.js && node --check lib/auth.mjs && node --check lib/content.mjs && node --check lib/football/api-football.mjs && node --check lib/football/service.mjs && node --check worker/index.mjs",
    "test": "node --test",
    "build:web": "npm run build",
    "cap:sync": "npm run build:web && cap sync android",
    "android:open": "cap open android",
    "android:run": "cap run android",
    "android:debug": "npm run cap:sync && cd android && ./gradlew assembleDebug",
    "android:bundle": "npm run cap:sync && cd android && ./gradlew bundleRelease",
    "worker:dev": "wrangler dev",
    "worker:test": "node --test test/worker.test.mjs",
    "worker:deploy": "wrangler deploy --env production"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.3",
    "@capacitor/app": "^7.0.2",
    "@capacitor/browser": "^7.0.2",
    "@capacitor/core": "^7.4.3",
    "@capacitor/network": "^7.0.2",
    "@capacitor/splash-screen": "^7.0.3",
    "@capacitor/status-bar": "^7.0.3"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.3",
    "wrangler": "^4.28.1"
  }
}
